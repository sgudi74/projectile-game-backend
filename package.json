{
  "name": "projectile-game-backend",
  "version": "1.0.0",
  "description": "Server for Projectile Game High Scores",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "cors": "^2.8.5",
    // Make sure you have REMOVED the old 'pg', 'sequelize', etc.
  }
}
